using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ammunition : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
    //void OnTriggerEnter(Collider col)
    //{

    //    if (col.gameObject.name == "gun")//albo  (col.gameObject.tag == "Player")
    //    {
    //        if (!(col.gameObject.GetComponent<UsingInstantiate>().getAmmunition() == col.gameObject.GetComponent<UsingInstantiate>().getMaxAmmunition()))
    //        {
    //            col.gameObject.GetComponent<UsingInstantiate>().AddAmmunition(20);
    //            Destroy(gameObject);
    //        }

    //    }

    //}
}
